CREATE TABLE employee (
    emp_id      TEXT NOT NULL,
    name        TEXT NOT NULL,
    contact_num TEXT,
    password    TEXT NOT NULL,
    address     TEXT,
    role        TEXT,
    PRIMARY KEY (
        emp_id
    )
);
